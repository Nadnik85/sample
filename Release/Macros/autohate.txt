#why is this a .txt?
sub main

/declare AutoHater int outer
/call 
/return AutoHater

Sub FindAutoHate
	/declare i int local
	/for i 1 to 10
		/if (${Me.XTarget[${i}].Type.Equal[Auto Hater]}) {
			/varset AutoHater ${i}
			/echo ${i}
		}
	/next i
/return


